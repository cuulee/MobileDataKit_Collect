<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MobileDataKit_Collect.App.Views.FormHome">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Button Grid.Row="0" Text="Fill Blank Form" Command="{Binding FillBlankFormCommand}"></Button>
            <Button Grid.Row="1" Text="Edit Saved Form"></Button>
            <Button Grid.Row="2" Text="Delete Saved Form"></Button>
            
        </Grid>
    </ContentPage.Content>
</ContentPage>